{
  "name": "lesny-quiz",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Leśny Quiz - frontend (Vite/React) + Vercel Serverless Functions + Neon Postgres",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build",
    "db:check": "node scripts/check-db.js",
    "db:setup": "node scripts/setup-db.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": "20.x"
  }
}
